# Copy this file to .env and add your real key there.
BROLL_PROMPT_PROVIDER=openrouter
OPENROUTER_API_KEY=your_openrouter_api_key
OPENROUTER_MODEL=model_name
# Optional: comma-separated fallbacks if the primary model is rate-limited.
OPENROUTER_FALLBACK_MODELS=model_name
